const {
  Events,
  EmbedBuilder,
  ActionRowBuilder,
  ButtonBuilder,
  ButtonStyle,
  ComponentType,
} = require('discord.js');
const { prefix, giveawayPrefix, tachhirPrefix } = require('../../config.js')

module.exports = {
  name: Events.InteractionCreate,
  /**
   * @param {import('discord.js').ButtonInteraction} interaction
   */
  async execute(interaction) {
    // نتأكد من أنه زر من صف الهيلب
    if (!interaction.isButton()) return;
    if (!interaction.customId.startsWith('help')) return;

    try {
        if(interaction.customId === "helpDelete") return interaction.message.delete().catch(() => {});
        // مصفوفة الأقسام مع العناوين والوصفات
    const pages = {
      helpHome: {
        title: 'Shop Help | الرئيسية',
        description: `-------------------------
## الأقسام :
🏠 الرئيسية** | هذه الصفحة**
📚 العامة** | الاوامر العامة الخاصة بالبوت**
🚨 تحذير البائعين** | الأوامر و المعلومات الخاصة بتحذير البائعين**
📋 الطلبات** | الأوامر و المعلومات الخاصة بنظام الطلبات**
🎁 جيف اوي** | الأوامر الخاصة بالجيف اوي**
🔔 تشهير النصابين** | الأوامر و المعلومات الخاصة بتشهير النصابين**
🛠️ الإدارة** | الأوامر و المعلومات الخاصة بالإداريين**
👑 الأونرز** | الأوامر و المعلومات الخاصة بالأونرز**`
      },
      helpGeneral: {
        title: 'Help | الأوامر العامة',
        description: `-------------------------
- \`${prefix}buy\` :
- - لشراء منتج من السيرفر (رتب عادية او فخمة / ازالة تحذير / منشور مميز / روم خاصة / اعلان).
- - الرومات الخاصة تحتوي على نظام تجديد تلقائيا
- - بعد انتهاء ال7 ايام يتم اخفاء الروم الخاصة 
- - و يطلب من صاحب الروم تحويل الكريدت في غضون 2 ايام
- - اذا حول يتم اظهار الروم و اذا لم يحول تنحذف الروم تلقائيا
- - كما تحتوي الرومات الخاصة على ميزة تغيير اسم التكت

- \`${prefix}tax [amount] {percentage}\` :
- - لمعرفة ضريبة البروبوت.`
      },
      helpWarnSeller: {
        title: 'Help | تحذير البائعين',
        description: `-------------------------
- - context menu يتم تحذير البائعين عبر اوامر الـ 
- - apps يعني ان الاداري يضغط على المنشور و يختار  
- - و بعدها يختار اما تحذير البائع او سحب رتبته ثم يكتب السبب فقط`
      },
      helpOrder: {
        title: 'Help | الطلبات',
        description: `-------------------------
- \`/ارسال-بانل-الطلبات\` :
- - لارسال رسالة نظام الطلبات
- - بامكان الاداري حذف الطلبات عن طريق الضغط على زر الحذف تحت اي رسالة طلب
- - ويكتب سبب الحذف فقط`
      },
      helpGiveaway: {
        title: 'Help | جيف أواي',
        description: `-------------------------
- \`${giveawayPrefix}start\` : لإنشاء جيف اوي في سيرفرك
- \`${giveawayPrefix}reroll\` : لإختيار فائز اخر
- \`${giveawayPrefix}end\` : لإنهاء جيف اوي
- \`${giveawayPrefix}pause\` : لإيقاف جيف اوي
- \`${giveawayPrefix}unpause\` : لإعادة تشغيل جيف اوي متوقف
- \`${giveawayPrefix}delete\` : لحذف جيف اوي`
      },
      helpTachhir: {
        title: 'Help | تشهير النصابين',
        description: `
- \`تشهير\` :
- - لإرسال رسالة تحتوي على مودل لاضافة نصاب للقائمة
- - بعد تشهير العضو سيعطيه البوت رتبة __\`نصاب\`__ تلقائيا 
- - و لن يستطيع رؤية اي روم في السيرفر
- - حتى بعد الخروج و الدخول للسيرفر ستبقى رتبة  النصاب

- \`فحص [mention user]\` :
- - لفحص شخص ما موجود في قائمة النصابين او لا

- \`ازالة [mention user]\` :
- - لازالة نصاب من قائمة النصابين (تتم الا من قبل مسؤول القضاة)`
      },
      helpMod: {
        title: 'Help | الإدارة',
        description: `-------------------------
- \`${prefix}نقط {mention user}\` :
- - لمعرفة نقاطك او نقاط اي اداري
- - النقاط يتم تجميعها عبر استلام التذاكر
- - التحذيرات لا تضيف نقاط تلقائيا

- \`${prefix}warn(+) [mention user] [number]\` :
- - لإضافة نقطة تحذير لأي إداري

- \`${prefix}warn(-) [mention user] [number]\` :
- - لإزالة نقطة تحذير لأي إداري

- \`${prefix}ticket(+) [mention user] [number]\` :
- - لإضافة نقطة تكت لأي إداري

- \`${prefix}ticket(-) [mention user] [number]\` :
- - لإزالة نقطة تكت لأي إداري

- \`${prefix}تصفير-اسبوعي [mention user]\` :
- -  لتصفير النقاط الأسبوعية لأي إداري تتم فقط من قبل مسؤول الإدارة`
      },
      helpOwners: {
        title: 'Help | الأونرز',
        description: `-------------------------
- \`${prefix}تشفير\` : 
- - لإرسال بانل التشفير.
 
- \`${prefix}تكت_دعم\` :
- - لإرسال تكت الدعم الفني.
- - تحتوي التكت على أزرار ( الشراء / مساعد للادارة / الاستلام / حذف التكت )
- - تحتوي على سلكت منيو للاسئلة الشائعة (faq) 
- - مساعد الادارة يحتوي على عديد الخيارات وهي :
- - -  اضافة او حذف عضو
- - -  تغيير اسم التكت / نقل رتب البيع الى حساب ثاني
- - - اعطاء او حذف بلاك ليست / نداء الادارة العليا 

- \`${prefix}privateRoomsCount\` :
- - لإرسال رسالة عدد الرومات الخاصة المتوفرة.

- \`${prefix}send-rules\` :
- - لإرسال بانل القوانين (السيرفر/البائعين/الإدارة)
- \`${prefix}send-info\` :
- -  لإرسال بانل المعلومات (الرتب / الرومات الخاصة / المنشورات ...)`
      },
    };

    // بناء الإيمبد الجديد
    const page = pages[interaction.customId];
    const embed = new EmbedBuilder()
      .setTitle(page.title)
      .setDescription((interaction.customId == "helpHome") || (interaction.customId == "helpDelete") ? page.description : page.description + "\n-# [] : مهم (يجب ادخاله) / {] : اختياري")
      .setColor('Red');

    // بناء صف الأزرار مع تفعيل وتعطيل حسب الزر المختار
    const buttons = [
      ['helpDelete','حذف','🗑️',ButtonStyle.Danger,false],
      ['helpHome','الرئيسية','🏠',ButtonStyle.Primary],
      ['helpGeneral','العامة','📚',ButtonStyle.Secondary],
      ['helpWarnSeller','تحذير البائعين','🚨',ButtonStyle.Secondary],
      ['helpOrder','الطلبات','📋',ButtonStyle.Secondary]
    ].map(([id,label,emoji,style,disabled]) =>
      new ButtonBuilder()
        .setCustomId(id)
        .setLabel(label)
        .setEmoji(emoji || null)
        .setStyle(id === "helpDelete" ? ButtonStyle.Danger : (id === interaction.customId) && (id !== "helpDelete") ? ButtonStyle.Primary : ButtonStyle.Secondary)
        .setDisabled(id === interaction.customId)
    );

    const moreButtons = [
      ['helpGiveaway','جيف اوي','🎁'],
      ['helpTachhir','تشهير النصابين','🔔'],
      ['helpMod','الإدارة','🛠️'],
      ['helpOwners','الأونرز','👑']
    ].map(([id,label,emoji]) =>
      new ButtonBuilder()
        .setCustomId(id)
        .setLabel(label)
        .setEmoji(emoji)
        .setStyle(id === interaction.customId ? ButtonStyle.Primary : ButtonStyle.Secondary)
        .setDisabled(id === interaction.customId)
    );

    const row1 = new ActionRowBuilder().addComponents(...buttons);
    const row2 = new ActionRowBuilder().addComponents(...moreButtons);

    // تحديث الرسالة
    await interaction.update({ embeds: [embed], components: [row1, row2] }); 
    } catch (error) {
      console.error(error);
    }
  }
};
